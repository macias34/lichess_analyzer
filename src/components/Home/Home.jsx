import Search from "../Search/Search";

const Home = () => {
  return (
    <div className="h-screen w-screen flex items-center justify-around flex-col">
      <Search />
    </div>
  );
};

export default Home;
